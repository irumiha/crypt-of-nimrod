# Chapter 2 — Nim for People Who Write Java for Money

Game state at the end of this chapter: the Chapter 1 title scene plus a
field of golden embers rising behind the crown. Also home of the
language tour (`nimble tour`).

Build and run: `nimble run`

## Changes from ch01

| File | Status | Notes |
|------|--------|-------|
| `src/embers.nim` | new | first homegrown module: Ember value type, spawn/update/draw, swap-and-shrink removal |
| `src/tour.nim` | new | the chapter's language snippets as one runnable program (pure stdlib) |
| `src/crypt_of_nimrod.nim` | changed | ember field state + spawn timer; `randomize()`; embers drawn behind the crown |
| `crypt_of_nimrod.nimble` | changed | added the `tour` task |
| `assets/` | unchanged | |
